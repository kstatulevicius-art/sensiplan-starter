// Only the fragment relevant to DailyModal props typing.
// Replace your existing DailyModal call with the below if needed.

/*
<DailyModal
  open={modalOpen}
  onClose={()=>setModalOpen(false)}
  initial={{
    id: selectedId,
    bbt: selectedEntry?.bbt,
    bleeding: selectedEntry?.bleeding,
    mucusSensation: selectedEntry?.mucusSensation,
    mucusAppearance: selectedEntry?.mucusAppearance,
    notes: selectedEntry?.notes,
    // Accept engine-shaped events without strict typing conflicts
    coitus: selectedEntry?.coitus as any
  }}
  onSave={upsertDay}
/>
*/
